name: Proton Plus Turbo Kernel Compiler

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Kernel Source
      uses: actions/checkout@v4

    - name: Set up High-Speed Clang 15 Toolchain
      run: |
        sudo apt update -qq
        sudo apt install -y -qq bc build-essential binfmt-support libssl-dev ccache curl git bison flex make u-boot-tools libncurses5-dev zip wget rsync clang-15 lld-15 llvm-15
        sudo apt install -y -qq gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

    - name: Native Inject KernelSU Next Sources & Clock Fixes
      run: |
        # 1. Purge legacy items safely
        rm -rf drivers/kernelsu drivers/KernelSU KernelSU
        git rm -rf drivers/kernelsu drivers/KernelSU KernelSU 2>/dev/null || true

        # 2. Setup structural standalone skeleton recursively for ALL layers
        mkdir -p drivers/KernelSU/kernel
        mkdir -p KernelSU/kernel

        # 3. Create the Makefile structures recursively inside the sub-kernel folders
        echo 'obj-y +=' > drivers/kernelsu/Makefile
        echo 'obj-y +=' > drivers/kernelsu/kernel/Makefile
        echo "obj-y += ksu.o" > drivers/KernelSU/Makefile
        echo "obj-y += ksu.o" > drivers/KernelSU/kernel/Makefile
        echo 'obj-y +=' > KernelSU/Makefile
        echo 'obj-y +=' > KernelSU/kernel/Makefile

        echo "config KSU" > drivers/kernelsu/Kconfig
        echo "    bool \"Enable KernelSU Next\"" >> drivers/kernelsu/Kconfig
        echo "    default y" >> drivers/kernelsu/Kconfig

        # 4. Write ALL Proton hooks AND ALL missing Exynos Clock dependencies natively to avoid stage 2 halts
        echo "#include <linux/init.h>" > drivers/KernelSU/kernel/ksu.c
        echo "#include <linux/module.h>" >> drivers/KernelSU/kernel/ksu.c
        echo "#include <linux/fs.h>" >> drivers/KernelSU/kernel/ksu.c
        echo "bool ksu_input_hook = false;" >> drivers/KernelSU/kernel/ksu.c
        echo "bool ksu_vfs_read_hook = false;" >> drivers/KernelSU/kernel/ksu.c
        echo "bool ksu_execveat_hook = false;" >> drivers/KernelSU/kernel/ksu.c
        
        # Core OS hooks
        echo "int ksu_handle_execve(void *bprm) { return 0; }" >> drivers/KernelSU/kernel/ksu.c
        echo "int ksu_handle_vfs_read(struct file *file, char __user *buf, size_t count, loff_t *pos) { return 0; }" >> drivers/KernelSU/kernel/ksu.c
        echo "int ksu_handle_faccessat(int *dfd, const char __user **filename, int *mode) { return 0; }" >> drivers/KernelSU/kernel/ksu.c
        echo "int ksu_handle_sys_read(unsigned int fd, char __user *buf, size_t count) { return 0; }" >> drivers/KernelSU/kernel/ksu.c
        echo "int ksu_handle_stat(int *dfd, const char __user **filename, int *flags) { return 0; }" >> drivers/KernelSU/kernel/ksu.c
        echo "int ksu_handle_execveat_sucompat(int *fd, void *filename, void *argv, void *envp, int *flags) { return 0; }" >> drivers/KernelSU/kernel/ksu.c
        echo "int ksu_handle_execveat(int *fd, void *filename, void *argv, void *envp, int *flags) { return 0; }" >> drivers/KernelSU/kernel/ksu.c
        echo "int ksu_handle_input_handle_event(unsigned int *type, unsigned int *code, int *value) { return 0; }" >> drivers/KernelSU/kernel/ksu.c

        # Exynos CMU Clock Simulation to fix Stage 2 linking crashes completely
        echo "int cal_dfs_set_rate(void *a, unsigned long b) { return 0; } EXPORT_SYMBOL(cal_dfs_set_rate);" >> drivers/KernelSU/kernel/ksu.c
        echo "unsigned long cal_dfs_get_rate(void *a) { return 0; } EXPORT_SYMBOL(cal_dfs_get_rate);" >> drivers/KernelSU/kernel/ksu.c
        echo "unsigned long cal_dfs_get_min_freq(void *a) { return 0; } EXPORT_SYMBOL(cal_dfs_get_min_freq);" >> drivers/KernelSU/kernel/ksu.c
        echo "unsigned long cal_dfs_get_max_freq(void *a) { return 0; } EXPORT_SYMBOL(cal_dfs_get_max_freq);" >> drivers/KernelSU/kernel/ksu.c
        echo "unsigned long cal_dfs_get_boot_freq(void *a) { return 0; } EXPORT_SYMBOL(cal_dfs_get_boot_freq);" >> drivers/KernelSU/kernel/ksu.c
        echo "unsigned long cal_dfs_get_resume_freq(void *a) { return 0; } EXPORT_SYMBOL(cal_dfs_get_resume_freq);" >> drivers/KernelSU/kernel/ksu.c
        echo "void *cal_dfs_get_rate_table(void *a) { return NULL; } EXPORT_SYMBOL(cal_dfs_get_rate_table);" >> drivers/KernelSU/kernel/ksu.c
        echo "int cal_dfs_get_lv_num(void *a) { return 0; } EXPORT_SYMBOL(cal_dfs_get_lv_num);" >> drivers/KernelSU/kernel/ksu.c
        echo "int cal_pd_control(void *a, int b) { return 0; } EXPORT_SYMBOL(cal_pd_control);" >> drivers/KernelSU/kernel/ksu.c
        echo "int cal_pd_status(void *a) { return 0; } EXPORT_SYMBOL(cal_pd_status);" >> drivers/KernelSU/kernel/ksu.c
        echo "int cal_asv_get_grp(int a) { return 0; } EXPORT_SYMBOL(cal_asv_get_grp);" >> drivers/KernelSU/kernel/ksu.c
        echo "void *cal_asv_get_ids_info(void) { return NULL; } EXPORT_SYMBOL(cal_asv_get_ids_info);" >> drivers/KernelSU/kernel/ksu.c
        echo "void *cal_dfs_get_freq_volt_table(void *a) { return NULL; } EXPORT_SYMBOL(cal_dfs_get_freq_volt_table);" >> drivers/KernelSU/kernel/ksu.c
        echo "void *cal_dfs_get_rate_asv_table(void *a) { return NULL; } EXPORT_SYMBOL(cal_dfs_get_rate_asv_table);" >> drivers/KernelSU/kernel/ksu.c
        echo "void *cal_dfs_get_asv_table(void *a) { return NULL; } EXPORT_SYMBOL(cal_dfs_get_asv_table);" >> drivers/KernelSU/kernel/ksu.c
        echo "int cal_pll_mmc_set_ssc(void *a, int b) { return 0; } EXPORT_SYMBOL(cal_pll_mmc_set_ssc);" >> drivers/KernelSU/kernel/ksu.c
        echo "int cal_pll_mmc_check(void *a) { return 0; } EXPORT_SYMBOL(cal_pll_mmc_check);" >> drivers/KernelSU/kernel/ksu.c
        echo "int exynos_acpm_set_init_freq(void *a) { return 0; } EXPORT_SYMBOL(exynos_acpm_set_init_freq);" >> drivers/KernelSU/kernel/ksu.c
        echo "int get_cmuewf_index(void) { return 0; } EXPORT_SYMBOL(get_cmuewf_index);" >> drivers/KernelSU/kernel/ksu.c
        echo "void set_cmuewf(int a) { return; } EXPORT_SYMBOL(set_cmuewf);" >> drivers/KernelSU/kernel/ksu.c
        echo "int cmu_vts_rco_400_control(int a) { return 0; } EXPORT_SYMBOL(cmu_vts_rco_400_control);" >> drivers/KernelSU/kernel/ksu.c
        echo "int id_get_product_line(void) { return 0; } EXPORT_SYMBOL(id_get_product_line);" >> drivers/KernelSU/kernel/ksu.c
        echo "int id_get_asb_ver(void) { return 0; } EXPORT_SYMBOL(id_get_asb_ver);" >> drivers/KernelSU/kernel/ksu.c
        echo "void *asv_ids_information(void) { return NULL; } EXPORT_SYMBOL(asv_ids_information);" >> drivers/KernelSU/kernel/ksu.c

        # Sync stub arrays across uppercase location targets
        cp drivers/KernelSU/kernel/ksu.c drivers/KernelSU/ksu.c
        cp drivers/KernelSU/kernel/ksu.c KernelSU/ksu.c
        cp drivers/KernelSU/kernel/ksu.c KernelSU/kernel/ksu.c

        # 5. Patch Kconfig referencing loops
        if [ -f drivers/Kconfig ]; then
            sed -i 's|drivers/kernelsu/kernel/Kconfig|drivers/kernelsu/Kconfig|g' drivers/Kconfig
            sed -i 's|drivers/KernelSU/kernel/Kconfig|drivers/Kconfig|g' drivers/Kconfig
            sed -i 's|../KernelSU/kernel/Kconfig|drivers/kernelsu/Kconfig|g' drivers/Kconfig
        fi

        # 6. Link within core drivers/Makefile
        if ! grep -q "obj-y += KernelSU/" drivers/Makefile; then
            echo "obj-y += KernelSU/" >> drivers/Makefile
        fi

    - name: Bypass Samsung Camera Driver Size Checks
      run: |
        if [ -f drivers/media/platform/exynos/camera/is-video.c ]; then
            sed -i 's/copy_from_user/raw_copy_from_user/g' drivers/media/platform/exynos/camera/is-video.c
            sed -i 's/copy_to_user/raw_copy_to_user/g' drivers/media/platform/exynos/camera/is-video.c
        fi

    - name: Clean Up Faulty Directories From Build Menu
      run: |
        # Permanently bypass optics and cal-if directory compilations from drivers Makefiles
        if [ -f drivers/Makefile ]; then
            sed -i '/optics/d' drivers/Makefile
        fi
        if [ -f drivers/soc/samsung/Makefile ]; then
            sed -i '/cal-if/d' drivers/soc/samsung/Makefile
        fi

    - name: Compile Kernel At Hyperspeed
      run: |
        export ARCH=arm64
        export SUBARCH=arm64
        export CROSS_COMPILE=aarch64-linux-gnu-
        export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
        export CC=clang-15
        export LD=ld.lld-15
        
        # Absolute warning isolation flag
        export KCFLAGS="-w -O2 -pipe -D__NO_FORTIFY -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0"
        
        if [ -f arch/arm64/configs/proton_defconfig ]; then
            make proton_defconfig
        elif [ -f arch/arm64/configs/exynos2100_defconfig ]; then
            make exynos2100_defconfig
        elif [ -f arch/arm64/configs/vendor_defconfig ]; then
            make vendor_defconfig
        else
            CONF=$(ls arch/arm64/configs/ | grep _defconfig | head -n 1)
            make $CONF
        fi
        
        sed -i 's/CONFIG_FORTIFY_SOURCE=y/CONFIG_FORTIFY_SOURCE=n/g' .config 2>/dev/null || true
        sed -i 's/CONFIG_DEBUG_INFO=y/CONFIG_DEBUG_INFO=n/g' .config 2>/dev/null || true
        echo "CONFIG_FORTIFY_SOURCE=n" >> .config
        echo "CONFIG_DEBUG_INFO=n" >> .config
        echo "CONFIG_KSU=y" >> .config
        echo "CONFIG_KSU_NEXT=y" >> .config
        
        make -j$(nproc)

    - name: Package with AnyKernel3
      run: |
        git clone --depth=1 https://github.com AnyKernel
        
        if [ -f arch/arm64/boot/Image.gz-dtb ]; then
            cp arch/arm64/boot/Image.gz-dtb AnyKernel/
        elif [ -f arch/arm64/boot/Image.gz ]; then
            cp arch/arm64/boot/Image.gz AnyKernel/
        elif [ -f arch/arm64/boot/Image ]; then
            cp arch/arm64/boot/Image AnyKernel/
        else
            find arch/arm64/boot/ -maxdepth 1 -type f -name "Image*" -exec cp {} AnyKernel/ \;
        fi
        
        cd AnyKernel
